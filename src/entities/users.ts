export type ProtoUser = {
    name?: string;
    email?: string;
    passwd?: string;
    role?: string;
};

export type User = {
    id: string;
    name: string;
    email: string;
    passwd: string;
    role: string;
};
